# Required. OpenRouter API key for LLM and embedding calls.
OPENROUTER_API_KEY=

# Optional. Overrides the default chat model (DeepSeek v4 flash).
OPENROUTER_MODEL=

# Optional. Enables LangSmith tracing. Tracing is off when this is empty.
LANGSMITH_API_KEY=
