{
  "name": "future-knowledge-graph-coach-dashboard",
  "private": true,
  "packageManager": "pnpm@10.34.5",
  "scripts": {
    "dev": "./scripts/dev.sh",
    "dev:api": "uv run --project backend uvicorn app.main:app --reload --port 8000 --app-dir backend",
    "dev:web": "pnpm --dir frontend dev",
    "lint": "uv run --project backend ruff check backend && uv run --project backend ruff format --check backend && uv run --directory backend ty check && pnpm --dir frontend lint",
    "seed": "uv run --directory backend python -m app.graph.seed",
    "test": "uv run --directory backend pytest && pnpm --dir frontend test"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
